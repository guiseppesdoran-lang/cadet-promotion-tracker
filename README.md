# CAP Cadet Promotion Requirement Checker

This tool reads a CSV export from eServices (Cadet Full Track Report) and generates a simple report showing **what each cadet still needs** in order to promote.

---

## 🚀 Features
- Upload your **Cadet Full Track CSV**
- Automatically evaluates:
  - Required dates
  - Required scores
  - TRUE/FALSE requirements
- Treats `N/A` as automatically satisfied
- Generates a clean file: `promotion_report.csv`
- Shows a simple list of requirements each cadet is missing

---

## 📦 Installation

```bash
pip install -r requirements.txt
